{{!-- Site header --}}
<header>
  {{!-- Default navigation --}}
  <nav class="navbar navbar__default">
    <div class="navbar-header container">
      <div class="col-xs-3 col-sm-3 navbar-search">
        <div class="navbar-search btn-search--open">
          <div class="navbar-search__group">
            <i class="fa fa-search"></i>
            <p class="hidden-xs">{{t "Search"}}</p>
          </div>
        </div>
      </div>
      {{!-- Blog logo or name --}}
      <div class="col-xs-6 col-sm-6 navbar-logo">
        <a href="{{@site.url}}">
          {{#if @site.logo}}
            <img class="lazyload" data-src="{{@site.logo}}" alt="{{@site.title}}">
          {{else}}
            <h3 class="navbar-title">{{@site.title}}</h3>
          {{/if}}
        </a>
      </div>
      {{!-- Menu button --}}
      <div class="col-xs-2 col-sm-2 navbar-menu">
        <button class="btn-navbar--open button--light pull-right">
          <span class="active-page hidden-xs">{{t "Menu"}}</span>
          <i class="fa fa-bars"></i>
        </button>
      </div>
      {{!-- Language Switch --}}
      <div class="col-xs-1 col-sm-1 language-switch">
        <p style="font-size:50%"> </p>
        <strong style="font-size:60%; color: white; margin-left:3px">ES</strong>
        <a style="font-size:60%; color:white" href="https://unisal.org"> EN</a>
      </div>
    </div>
    <div class="navbar-navigation">
      <a href="" class="btn--close btn-navbar--close">
        <span class="hidden">{{t "Close"}}</span>
      </a>
      {{!-- Blog navigation --}}
      {{navigation}}
    </div>
  </nav>
